import Component from '@ember/component';

export default Component.extend({
  tagName: 'div',

  classNames: ['device-list', 'layout-wrap', 'layout-row', 'layout-align-center-center'],

  classNameBindings: [
  ]
});
